{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DetailsProduits list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Refprod</th>
                <th>Libelle</th>
                <th>Prixht</th>
                <th>Quantite</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('detailsproduits_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.refProd }}</td>
                <td>{{ entity.libelle }}</td>
                <td>{{ entity.prixHT }}</td>
                <td>{{ entity.quantite }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('detailsproduits_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('detailsproduits_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('detailsproduits_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

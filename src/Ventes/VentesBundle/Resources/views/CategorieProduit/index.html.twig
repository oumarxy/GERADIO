{% extends "VentesBundle::layout_ventes.html.twig" %}

{% block container_ventes %}  
    <h3>CategorieProduit list</h3>
  <div class="row">
        <div class="col-md-7">
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('categorieproduit_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td>{{ entity.libelle }}</td>
                     <td>{{ entity.prixHT|number_format(0,',',' ') }}</td>
                     <td>
                        <a class="btn btn-primary btn-xs" href="{{ path('categorieproduit_show', { 'id': entity.id }) }}"> <i class="fa fa-eye"></i> Voir </a>&nbsp;
                        <a class="btn btn-success btn-xs" href="{{ path('categorieproduit_edit', { 'id': entity.id }) }}"> <i class="fa fa-edit"></i>  Modifier</a>
                </tr>
            {% endfor %}
        </tbody>
    </table>
        </div>
        </div>
  
            <a  class="btn btn-primary" href="{{ path('categorieproduit_new') }}">
                Nouvelle Catégorie
            </a>
      
{% endblock %}

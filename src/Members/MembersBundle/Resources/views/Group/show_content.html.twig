{% trans_default_domain 'FOSUserBundle' %}

<div class="panel">
    <p>{{ 'group.show.name'|trans }}: {{ group.getName() }}</p>
</div>

<div class="panel">
    <a href="{{path('fos_user_group_list')}}"><i class="fa fa-circle-o text-red"></i> 
		<span>Retour</span>
	</a>
</div>

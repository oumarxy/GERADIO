{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_new">
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-primary" type="submit" value="{{ 'group.new.submit'|trans }}" />

        <a href="{{path('fos_user_group_list')}}" class="btn btn-primary float-left">
		<span>Annuler</span>
	</a>
    </div>
</form>
<br>

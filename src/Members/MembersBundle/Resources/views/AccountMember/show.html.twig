{% extends 'MembersBundle::layout_member.html.twig' %}

{% block container_member %} 
    <h1>Détails utilisateur</h1>
    <div class="divider"></div>
    <div class="row">
    <div class="col-md-7">
    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ entity.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
               <td>{% for role in entity.roles %} {{ role }} <br /> {% endfor %}</td>
            </tr>
        </tbody>
    </table>
    </div>
    </div>
      
    <a class="btn btn-success float-left" href="{{ path('account') }}">
        Liste utilisateurs
    </a>
        <a  class="btn btn-primary" href="{{ path('account_edit', { 'id': entity.id, 'status':'infoperso' }) }}">
            Modifier
        </a>
        <a  class="btn btn-primary" href="{{ path('account_edit', { 'id': entity.id, 'status':'pwd' }) }}">
            Changer le Mot de passe
        </a>
   
{% endblock %}

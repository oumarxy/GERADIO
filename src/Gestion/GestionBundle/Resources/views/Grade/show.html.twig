{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Grade</h1>
 <div class="row">
        <div class="col-md-4">
    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libgrade</th>
                <td>{{ entity.libgrade }}</td>
            </tr>
        </tbody>
    </table>
</div>
</div>
        <a class="btn btn-success btn-xs" href="{{ path('grade') }}">
            Retour
        </a>
 
        <a class="btn btn-success btn-xs" href="{{ path('grade_edit', { 'id': entity.id }) }}">
            Modifier
{{ form(delete_form) }}
{% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Salaire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Periode</th>
                <td>{{ entity.periode }}</td>
            </tr>
            <tr>
                <th>Salairenet</th>
                <td>{{ entity.salairenet }}</td>
            </tr>
            <tr>
                <th>Totalgain</th>
                <td>{{ entity.totalgain }}</td>
            </tr>
            <tr>
                <th>Totalretenue</th>
                <td>{{ entity.totalretenue }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('salaire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('salaire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

{% extends 'GestionBundle::layout_gestion.html.twig' %}

{% block container_gestion %}
    <h1>Cvupload</h1>
  <div class="row">
        <div class="col-md-5">
    <table class="table table-striped table-bordered">
        <tbody>
          
            <tr>
                <th>CV</th>
                <td>{{ entity.libellecv }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date("d/m/Y") }}</td>
            </tr>
        </tbody>
    </table>
</div>
</div>
 <a class="btn btn-success "  href="{{ path('cvupload', { 'idemp': entity.employe.id }) }}">
           Retour
        </a>
       
{% endblock %}
